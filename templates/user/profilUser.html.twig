{% extends 'base.html.twig' %}

{% block title %}Sortir.com{% endblock %}

{% block body %}
     <div class="row">
          <div class="col-sm"></div>
          <div class="col-sm">
               <h1>Mon profil</h1>
               {{ form_start(profilUserForm) }}
               {{ form_widget(profilUserForm) }}
               {{ form_end(profilUserForm) }}
               <a class="btn btn-secondary" href="{{ path('home_home') }}">Annuler</a>
          </div>
          <img src="{{ app.user.photo }}">
          <div class="col-sm"></div>
     </div>

{% endblock body %}