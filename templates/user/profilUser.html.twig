{% extends 'base.html.twig' %}

{% block title %}Sortir.com{% endblock %}

{% block body %}
     <div class="col-sm py-2"></div>
     <div>
          <div class="col-12-sm py-5"></div>
          {# myProfil #}
          <div class="row">
               <div class="col-sm-1 "></div>
               <div class="col-sm-10 mx-0 px-0 py-0">
                    {# encadré de myProfil #}
                    <div class="viewProfil-conteneur">

                         <div class="py-5 ml-5 text-center">  <h1>Mon profil :  </h1>
                         </div>
                         <div class="row my-5 mx-5">
                              <div class="col-sm">
                                   {{ form_start(profilUserForm) }}
                                   {{ form_widget(profilUserForm) }}
                                   {{ form_end(profilUserForm) }}
                                   <a class="btn btn-secondary" href="{{ path('home_home') }}">Annuler</a>
                              </div>
                              <div class="col-sm myProfil-picture">
                                   <img src="{{ app.user.photo }}">
                              </div>
                         </div>
                    </div>
               </div>
          </div>
     </div>
{% endblock body %}